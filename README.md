## 1. 第一个agent为数据收集agent，

### 1.1 存放位置：
单独存放在main_2_fixed_output_fn.py文件中，可单独执行此文件

### 1.2大致流程：
1. 根据用户提供的公司名称/公司股票代码与对应年份，返回json，并向用户确认
2. 用户确认后，执行函数（爬取函数，下载对应文件）

### 1.3 测试输入
1. 我想要三全食品公司2020到2022年的财报数据
2. 确认

## 2. 第二个agent为分析agent

### 2.1 存放位置：
单独存放在main_4_agent2.py文件中，可单独执行此文件

### 2.2 大致流程：
根据固定的文件存放地址，提取文件内容，并结合用户的问题进行回复

### 2.3 测试输入
1. 三全食品这两年的利润怎么样

## 3. 总的执行文件，3个agent，其中一个路由agent决定使用哪个agent

### 3.1 存放位置：
单独存放在main_combin.py中

### 注： 此文件代码尚有问题，正在调试